# AI Skills 文档仓库

## 规则

**所有对话使用中文进行。**

## 目的

本仓库记录 AI 编码代理的技能和工具文档，主要面向 Claude Code 和 OpenCode。

## 目录结构

```
skills/           # 详细技能文档（10个文件）
skills.md         # 所有追踪技能的快速索引
coding plan.md    # 编程套餐价格对比（腾讯、阿里等）
Claude Code.md    # Claude Code 配置笔记
```

## 技能文档格式

每个技能文档包含：
1. 简介
2. 如何安装 - Claude Code、OpenCode、手动/离线安装方法
3. 如何使用 - 核心工作流和关键命令

## 核心技能快速参考

| 技能 | 主要用途 | 安装方式 |
|------|---------|---------|
| superpowers | TDD方法论 | OpenCode插件 |
| gstack | 23角色虚拟团队 | git clone + setup |
| pua | 压力驱动调试 | 单个SKILL.md |
| oh-my-openagent | 多智能体编排 | OpenCode插件 |
| ui-ux-pro-max | 设计系统生成 | npm CLI |

## 安装目录

- OpenCode: `~/.config/opencode/skills/`
- Claude Code: `~/.claude/skills/`

## 注意事项

- 所有技能都支持手动安装，适用于离线/企业网络环境

---

## 编码最佳实践（Karpathy Guidelines）

> 来源：[Andrej Karpathy](https://x.com/karpathy/status/2015883857489522876) 关于LLM编码陷阱的观察

**权衡：** 这些指南偏向谨慎而非速度。对于简单任务，使用判断力。

### 1. Think Before Coding（先思考后编码）

**不要假设。不要隐藏困惑。展示权衡。**

实现前：
- 明确陈述假设。如果不确定，问。
- 存在多种解释时全部呈现——不要默默选择。
- 如果存在更简单的方法，说出来。必要时反驳。
- 如果不清楚，停下来。说出困惑。问。

### 2. Simplicity First（简单优先）

**用最少的代码解决问题。不做推测性开发。**

- 不添加未被请求的功能。
- 单次使用的代码不做抽象。
- 不添加未被请求的"灵活性"或"可配置性"。
- 不处理不可能发生的错误场景。
- 如果200行可以变成50行，重写它。

问自己："高级工程师会说这太复杂了吗？"如果是，简化它。

### 3. Surgical Changes（外科手术式修改）

**只触碰必须触碰的。只清理自己制造的混乱。**

编辑现有代码时：
- 不要"改进"相邻代码、注释或格式。
- 不要重构没坏的东西。
- 匹配现有风格，即使你会用不同方式。
- 注意到无关死代码时，提及它——不要删除它。

你的修改产生孤立代码时：
- 移除你的修改导致未使用的导入/变量/函数。
- 不要移除预先存在的死代码，除非被要求。

测试标准：每一行修改都应该能追溯到用户的请求。

### 4. Goal-Driven Execution（目标驱动执行）

**定义成功标准。循环直到验证通过。**

转换任务为可验证目标：
- "添加验证" → "为无效输入写测试，然后让它们通过"
- "修复bug" → "写一个能复现它的测试，然后让它通过"
- "重构X" → "确保重构前后测试都通过"

多步骤任务格式：
```
1. [步骤] → verify: [检查方式]
2. [步骤] → verify: [检查方式]
3. [步骤] → verify: [检查方式]
```

强成功标准让你独立循环。弱标准（"让它工作"）需要不断澄清。

---

**这些指南有效的标志：** diff中更少不必要的更改，更少因过度复杂而重写，澄清问题在实现前而非犯错后出现。
